<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Android ignores <domain> for IP addresses, so 127.0.0.1 needs base-config. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
